apiVersion: v1
kind: Service
metadata:
  name: api-infrastructure-quarkus
  namespace: default
  labels:
    app: api-infrastructure-quarkus
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: api-infrastructure-quarkus
  type: NodePort
